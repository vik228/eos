schema_version: 1
command: migrate
command_path: ["migrate", "approve"]
owner: Task 13
argv: ["migrate", "approve", "--kb", "/tmp/kb", "--manifest", "/tmp/manifest.json", "--manifest-hash", "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa", "--approved-by", "reviewer", "--approval-session", "s1"]
cases:
  success:
    placeholder: true
  validation_failure:
    placeholder: true
  recovery:
    placeholder: true
    recovery: not_applicable
  output_shape:
    placeholder: true
    formats: [text, json]
