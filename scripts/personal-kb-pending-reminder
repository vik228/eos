#!/usr/bin/env bash
set -euo pipefail
ROOT="${EOS_ROOT:-$HOME/personal/eos}"
exec env EOS_CAPTURE_PROFILE=personal \
  EOS_CAPTURE_KB_ROOT="${EOS_PERSONAL_KNOWLEDGE_DIR:-${EOS_PERSONAL_KNOWLEDGE_ROOT:-$HOME/personal/knowledge}}" \
  "$ROOT/scripts/eos-kb-pending-reminder" "$@"
