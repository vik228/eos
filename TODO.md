# TODO

- Decide whether to symlink `~/.config/nvim` after reviewing the current Neovim config.
- Add casks to `brew/Brewfile` only when they are confirmed necessary.
- Expand future Python CLI under `eos/`.
