export EOS_ROOT="${EOS_ROOT:-$HOME/personal/eos}"

export EOS_ROOT="${EOS_ROOT:-$HOME/personal/eos}"
source "$EOS_ROOT/scripts/lib-eos-config.sh"

export EDITOR="nvim"
export VISUAL="nvim"
export PATH="$EOS_ROOT/scripts:$EOS_ROOT/bin:$HOME/.local/bin:/opt/homebrew/bin:/opt/homebrew/sbin:$PATH"
export STARSHIP_CONFIG="$EOS_ROOT/configs/starship/starship.toml"

if [[ -x /opt/homebrew/bin/brew ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

export PATH="$EOS_ROOT/scripts:$EOS_ROOT/bin:$HOME/.local/bin:$PATH"

source "$EOS_ROOT/configs/shell/aliases.zsh"
source "$EOS_ROOT/configs/shell/functions.zsh"

if command -v zoxide >/dev/null 2>&1; then
  eval "$(zoxide init zsh)"
fi

if command -v starship >/dev/null 2>&1; then
  eval "$(starship init zsh)"
fi

bindkey -e
